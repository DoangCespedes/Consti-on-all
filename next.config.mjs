/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['segurosconstitucion.com'], // Agrega el dominio permitido aquí
    },
  };
  
  export default nextConfig;
  